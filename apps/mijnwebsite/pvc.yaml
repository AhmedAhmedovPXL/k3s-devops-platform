apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: website-data
  namespace: mijnwebsite
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
