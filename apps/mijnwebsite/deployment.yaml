apiVersion: apps/v1
kind: Deployment
metadata:
  name: mijnwebsite
  namespace: mijnwebsite
spec:
  replicas: 4
  selector:
    matchLabels:
      app: mijnwebsite
  template:
    metadata:
      labels:
        app: mijnwebsite
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: website-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: website-content
        configMap:
          name: website-content
